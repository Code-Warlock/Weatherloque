{
  "name": "weather",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "compile:sass": "sass --watch sass:src/",
    "compile:postcss" : "npx postcss --watch src/weather.css --use autoprefixer -d build/",
    "dev" : "start npm run compile:sass && start npm run compile:postcss"
  },
  "author": "Code-Warlock",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.13",
    "postcss-cli": "^9.1.0",
    "sass": "^1.51.0",
    "sass-autoprefixer": "^1.0.1"
  }
}
